#ifndef SIGN
#define SIGN

/**
*Function to calculate the sign of the argument.
@param x argument to be evaluated
@return the sign o x: if x>0, +1, if x = 0, 0 and if x<0, -1
*/

int sign_arg(double x);

#endif
